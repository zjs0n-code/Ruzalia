DEVIL'S ADVOCATE CRITIQUE (Binary Eval)
═══════════════════════════════════════

Target: code changes on branch `nuclear-skin` (`git diff 0de00109..HEAD`, 4 commits, 66 files, +1899/-629) — a reskin of Metrolist in the "nuclear" desktop player's design language.

Verification performed:
- `./gradlew.bat :app:compileFossDebugKotlin --console=plain` → BUILD SUCCESSFUL, 0 `e:` lines, 0 `w:` lines.
- `./gradlew.bat :app:testFossDebugUnitTest --console=plain` → BUILD SUCCESSFUL; XML results aggregate to 137 tests, 0 failures, 0 errors, 0 skipped.
- Read all 6 new files in `ui/theme/nuclear/`, `ui/theme/Theme.kt`, and the full diff for every converted call site.
- Differential unused-import analysis (this branch vs `0de00109`) so only *newly* introduced dead imports are counted.
- Extracted `material3-android-1.5.0-alpha27` `classes.jar` and confirmed `IconButtonKt`/`SurfaceKt` reference `minimumInteractiveComponentSize` and `androidx.compose.ui.semantics.Role`.

  Correctness:
    tests-pass ...... PASS — `:app:testFossDebugUnitTest` BUILD SUCCESSFUL; 137/137 green across
                      `app/build/test-results/testFossDebugUnitTest/*.xml`. Compile is clean too.
    logic-correct ... FAIL — `NuclearButton` reserves height for its shadow via
                      `defaultMinSize(minHeight = size.height + shadowOffset)`
                      (NuclearButton.kt:108-113) and expects the face to fill it via
                      `faceFill = NuclearFaceFill.Height` (NuclearButton.kt:124), which resolves to
                      `Modifier.fillMaxHeight()` (NuclearSurface.kt:99). `fillMaxHeight` is a no-op
                      when `constraints.hasBoundedHeight` is false. Its only call site,
                      NavigationTitle.kt:88, sits in a wrap-content `Row`
                      (NavigationTitle.kt:50-59) that propagates an unbounded cross-axis max, so the
                      face wraps its ~16dp label inside a 34dp box while the shadow child uses
                      `matchParentSize()` (NuclearSurface.kt:87) and paints the full 34dp — leaving a
                      solid ~18dp band of shadow colour under every "Play all" button.
                      Fix: in `NuclearSurface`, stop relying on `fillMaxHeight`/`fillMaxSize` for
                      `faceFill` and instead size the face from the outer Box's measured size — e.g.
                      replace the enum with a `SubcomposeLayout`/custom `Layout` that measures the
                      face at `parentSize - shadowOffset`, or at minimum apply
                      `Modifier.heightIn(min = size.height)` to the face in `NuclearButton` so it can
                      never be shorter than its own shadow.
    edge-cases ...... FAIL — Two unhandled boundaries:
                      (1) `NuclearSurface` accepts `enabled` but applies it only to
                      `clickable(enabled = ...)` and the press animation (NuclearSurface.kt:78, 110);
                      there is no visual treatment. The transport buttons at Player.kt:1590
                      (`enabled = canSkipPrevious && !isListenTogetherGuest`) and Player.kt:1680
                      (`canSkipNext`) previously used `FilledIconButton`, whose
                      `IconButtonDefaults.filledIconButtonColors` supplies
                      `disabledContainerColor`/`disabledContentColor`. `sideButtonContainerColor` /
                      `sideButtonContentColor` (Player.kt:539-591) do not vary with skip
                      availability, so "cannot skip" is now completely invisible — a dead-looking
                      button with no feedback. `NuclearButton`/`NuclearIconButton` do apply
                      `.alpha(0.5f)` (NuclearButton.kt:112, 168), so the surface is the odd one out.
                      (2) Pure black: Theme.kt:56 sets `background` and `backgroundInput` to
                      `Color.Black`, and `NuclearPalette.shadow` returns `backgroundInput` when dark
                      (NuclearPalette.kt:87). In pure-black dark mode every hard shadow is
                      black-on-black, i.e. invisible — the skin's central visual device silently
                      disappears for that setting.
                      Fix: move the `.alpha(if (enabled) 1f else 0.5f)` (or a disabled colour pair)
                      into `NuclearSurface` itself so `enabled` means one thing package-wide; and in
                      Theme.kt keep the original `backgroundInput` (or fall back to `border`) when
                      computing `shadow` under pure black.

  Security:
    no-secrets ...... PASS — `git diff 0de00109..HEAD | grep -Ei 'api[_-]?key|secret|password|token|BEGIN (RSA|PRIVATE)'`
                      returns only prose matches inside KDoc ("tokens" as in design tokens). The
                      added binaries are three OFL fonts with their licences in
                      `app/src/main/assets/licenses/`.
    input-validated . PASS — The one new external input is the persisted `NuclearThemeKey` string
                      (PreferenceKeys.kt:18), read through `rememberEnumPreference`, which routes to
                      `String?.toEnum` (extensions/StringExt.kt:15-23) — null-safe and
                      `IllegalArgumentException`-safe with a default. `NullableColorSaver`
                      (Theme.kt:108-113) round-trips through `Int` with a `NO_COLOR` sentinel and
                      cannot throw.
    no-injection .... PASS — No SQL, no string-built queries, no WebView/JS, no `Runtime.exec`, no
                      new `Intent` action or URI construction from user data in the diff. The only
                      new `Intent` work is the pre-existing share sheet at Player.kt:1174-1184,
                      unchanged in substance.
    auth-enforced ... PASS — No protected operation is added or moved. The one behavioural gate that
                      exists, `isListenTogetherGuest`, is still passed to every converted control
                      (Player.kt:1590, 1613, 1680; Queue.kt:326, 344, 378), so guests still cannot
                      drive playback.

  Quality:
    no-dead-code .... FAIL — 20 imports became unused *on this branch* (verified differentially
                      against `0de00109`, so pre-existing cruft is excluded):
                      Player.kt:31 `BorderStroke` (added by this branch and never used);
                      NavigationTitle.kt:8,12,17,22,23,26 (`BorderStroke`, `PaddingValues`, `height`,
                      `RoundedCornerShape`, `ButtonDefaults`, `OutlinedButton`);
                      NewMenuComponents.kt:10,12,23,24 (`BorderStroke`, `clickable`, `Card`,
                      `CardDefaults`); Queue.kt:47 `CircleShape`, Queue.kt:84 `clip`;
                      ThemeScreen.kt:16,60,61,64,80 (`Canvas`, `Offset`, `Size`, `toArgb`,
                      `SelectedThemeColorKey`); MainActivity.kt:61 `IconButton`;
                      ChipsRow.kt:34 `RoundedCornerShape`.
                      Orphaned state: `SelectedThemeColorKey` now has **no writer** anywhere (the
                      colour picker was removed from ThemeScreen), yet AppearanceSettings.kt:155-161
                      still reads it to compute `isUsingCustomColor`, which at
                      AppearanceSettings.kt:960 hides the dynamic-theme row. Any legacy value makes
                      that row vanish permanently with no UI left to reset it.
                      Dead parameters: `SubscribeButton(outlineColor, onSurfaceColor)` and
                      `FavoriteButton(outlineColor, onSurfaceColor)` (MiniPlayer.kt:1044-1046,
                      1088-1090) are still computed and passed by callers but no longer read.
                      Redundant effect: MainActivity.kt:608-612 now keys on `enableDynamicTheme` and
                      does exactly what MainActivity.kt:614-619 already does for the same key.
                      Fix: run `./gradlew :app:lintFossDebug` (or IDE "Optimize Imports") over the
                      touched files; delete `SelectedThemeColorKey`, `isUsingCustomColor` and the
                      `if (!isUsingCustomColor)` guard, or restore a writer; drop the two dead
                      parameters from both MiniPlayer helpers and their call sites; delete the
                      duplicated `LaunchedEffect`.
    no-placeholders . PASS — `git diff 0de00109..HEAD -- '*.kt' | grep -E '^\+.*(TODO|FIXME|XXX|HACK|STUB)'`
                      returns nothing. Every new composable has a real body; no stubbed branches.
    error-handling .. PASS — No new `try`/`catch` is introduced
                      (`grep -E '^\+.*catch'` on the Kotlin diff is empty) and no existing handler is
                      weakened. MainActivity.kt:651-654 keeps its `catch (e: Exception)` and now
                      degrades to `null` (fall back to the preset primary) instead of a stale colour,
                      which is a coherent failure mode rather than a swallow.
    no-code-smell ... FAIL — `NuclearFaceFill` (NuclearSurface.kt:140) is a leaky abstraction by its
                      own documentation: "A Box sizes its children to their content… Any caller that
                      imposes a size… has to say so here." Correct rendering therefore requires every
                      caller to reason about `NuclearSurface`'s private two-Box/`matchParentSize`
                      layout, which is exactly the reasoning that went wrong at NavigationTitle.kt:88.
                      Compounding it, `enabled` has two different contracts inside one package —
                      visual + interactive in `NuclearButton`/`NuclearIconButton`
                      (NuclearButton.kt:112, 168), interactive-only in `NuclearSurface`
                      (NuclearSurface.kt:78, 110) — so a reader cannot predict behaviour from the
                      parameter name.
                      Fix: make the face fill the reserved box unconditionally via a custom `Layout`
                      that measures the child at `constraints - shadowOffset` and delete
                      `NuclearFaceFill` from the public API; unify `enabled` so all three entry
                      points apply the same disabled treatment.
    
  Performance:
    no-obvious-perf . FAIL — `NuclearSurface` unconditionally allocates a `MutableInteractionSource`,
                      subscribes `collectIsPressedAsState()` and starts an `animateDpAsState`
                      (NuclearSurface.kt:72-81) *before* it knows whether `onClick` is null. Every
                      non-clickable surface therefore pays for an `Animatable`, a `LaunchedEffect`
                      and a flow collector that can never fire. `GridItem` (Items.kt:428) is exactly
                      such a surface — `onClick` is never passed — and it is the item composable
                      behind 62 call sites across `LazyVerticalGrid`/`LazyRow` screens (Library,
                      Home, Charts, Album, Artist, Browse, Account…), so this lands in the scroll hot
                      path. AGENTS.md §"AI-only guidelines" 6 explicitly requires prioritising
                      performance and battery efficiency.
                      Fix: guard the interaction plumbing — `val pressed = if (onClick != null)
                      source.collectIsPressedAsState().value else false`, and skip
                      `animateDpAsState` entirely (use a plain `0.dp`) when `onClick == null` or
                      `!shadow`.

  Consistency:
    types-consistent  PASS — Compiles with zero warnings. `NuclearThemeId` is persisted as a String
                      and read back through `rememberEnumPreference`; `accentOverride: Color?`
                      threads consistently from MainActivity.kt:666 → Theme.kt:41 →
                      NuclearColorScheme.kt:34; `NullableColorSaver` matches the `Color?` state it
                      saves (MainActivity.kt:602).
    naming-matches .. PASS — New files are PascalCase matching their primary declaration, the package
                      `ui/theme/nuclear/` sits under the existing `ui/theme/` tree, composables are
                      PascalCase, `LocalNuclearPalette`/`LocalNuclearMetrics` follow the `Local*`
                      CompositionLocal convention already used by `LocalMenuState`,
                      `LocalPlayerConnection` etc., and new string resources are prefixed
                      `nuclear_*` / `cd_*` consistently with the existing `cd_pure_black_mode`.
    patterns-followed FAIL — Two concrete divergences:
                      (1) `IntegrationCard.kt:67-71` received only the `items.size == 1` branch of
                      the radius change (24dp → 12dp); `index == 0`, `index == items.size - 1` and
                      `else` are still 24dp/6dp, and it never got the
                      `border = BorderStroke(NuclearTheme.metrics.borderWidth, NuclearTheme.colors.border)`
                      or the `surfaceContainer` fill. Its two structural siblings —
                      Material3SettingsGroup.kt:70-92 and Menu.kt:40-55 — got all four branches plus
                      the border. Same component shape, three different results.
                      (2) The commit that "converts controls to nuclear buttons" leaves four controls
                      hand-rolling the chunk with a raw `.clip().background().border().clickable()`
                      chain — Player.kt:1268, 1288, 1326, 1753 — instead of using
                      `NuclearIconButton`/`NuclearSurface`, so those four get no press-into-shadow
                      feedback and no shadow at all while their siblings 60 lines away do.
                      Fix: apply the full four-branch shape + border treatment to
                      `IntegrationCard.kt`, and convert the four remaining `Box` chains in Player.kt
                      to `NuclearIconButton` as the rest of that file already was.

  Integration:
    imports-correct . PASS — `:app:compileFossDebugKotlin` resolves every symbol
                      (0 `e:` lines). Spot-checked that
                      `com.metrolist.music.ui.theme.nuclear.{NuclearIconButton, NuclearSurface,
                      NuclearFaceFill, NuclearTheme, NuclearButtonVariant, NuclearThemeId,
                      nuclearHardShadow, nuclearOutlineOverlay, nuclearBorder}` all resolve to real
                      declarations in the new package.
    tests-exist ..... FAIL — Zero tests were added. `find app/src/test -path '*theme*'` and
                      `-path '*nuclear*'` are both empty, against 137 existing JVM unit tests that
                      include UI-adjacent ones (`app/src/test/kotlin/com/metrolist/music/ui/player/PlaybackErrorTest.kt`).
                      The new code contains genuinely unit-testable pure functions with non-trivial
                      loops and accessibility-relevant contracts: `contrastRatio`
                      (NuclearColorScheme.kt:114), `Color.readableOn` (:125), `NuclearPalette.mutedText`
                      (:148), `NuclearPalette.contentColorOn` (:169) and `NuclearThemeId.palette`
                      (NuclearPalette.kt:100). None has a regression test, so nothing catches a
                      palette whose body text silently drops under 4.5:1.
                      Fix: add `app/src/test/kotlin/com/metrolist/music/ui/theme/nuclear/NuclearPaletteTest.kt`
                      asserting, for all 5 presets × light/dark, that
                      `contrastRatio(foreground, background) >= 4.5` and
                      `contrastRatio(mutedText(), background) >= 4.5`, that `readableOn` terminates
                      and meets its minimum where reachable, and that `contentColorOn` picks the
                      higher-contrast of ink/paper.
    no-regressions .. FAIL — Existing unit tests still pass, but two behavioural regressions are not
                      covered by any test:
                      (1) `app/build.gradle.kts:10` changes `baseApplicationId` from
                      `com.metrolist.music` to `com.ruzalia.music`, while
                      `app/src/main/res/xml/shortcuts.xml:11,21` still declare
                      `android:targetPackage="com.metrolist.music"` and
                      `app/src/debug/res/xml/shortcuts.xml:13,21` still declare
                      `com.metrolist.music.debug`. AGP does not rewrite `targetPackage`, so both
                      launcher long-press shortcuts (Search, Library) now point at a package that is
                      not installed and will fail to launch.
                      (2) The Material→Nuclear conversions drop `minimumInteractiveComponentSize()`
                      (48dp minimum touch target) and `Role.Button` semantics, both of which
                      `androidx.compose.material3.IconButtonKt` and `SurfaceKt` provide — confirmed
                      by extracting `material3-android-1.5.0-alpha27/classes.jar` and finding both
                      symbols referenced. `NuclearSurface` uses a bare
                      `Modifier.clickable(enabled, interactionSource, indication, onClick)`
                      (NuclearSurface.kt:109-115) with no `role` and no minimum size. Affected
                      controls now sized under 48dp include MainActivity.kt:1051, 1062, 1073, 1084
                      (four 36dp top-bar actions), HideOnScrollFAB.kt:61 (40dp), Player.kt:1159,
                      1187, 1203, 1236 (42dp), Player.kt:2143 (40dp), MiniPlayer.kt:1054, 1100
                      (40dp). TalkBack will no longer announce any of them as buttons.
                      Fix: parameterise `shortcuts.xml` — either hardcode `com.ruzalia.music` /
                      `com.ruzalia.music.debug`, or drop `targetPackage`/`targetClass` in favour of
                      an implicit action that the manifest already declares. In `NuclearSurface`, add
                      `.minimumInteractiveComponentSize()` to the outer Box and pass
                      `role = Role.Button` (plus an optional `onClickLabel`) whenever `onClick != null`.

  Architecture:
    boundaries-respected .. PASS — All new code lands in `ui/theme/nuclear/` and is consumed only
                      through `LocalNuclearPalette`/`LocalNuclearMetrics` + a `NuclearTheme` accessor
                      object that deliberately mirrors `MaterialTheme` (NuclearTheme.kt:44-54); the
                      Material `ColorScheme` remains the single source of truth for the existing ~35
                      colour roles (NuclearColorScheme.kt:57-110), so unconverted screens keep
                      working. Nothing reaches into `db/`, `playback/` or `innertube/`. AGENTS.md is
                      respected where the fork does not deliberately diverge: no DB schema change
                      (rule 5 — `git diff` touches no `db/` file), string edits confined to
                      `app/src/main/res/values/metrolist_strings.xml` (rule 3), no version bump
                      (rule 9 — `versionCode`/`versionName` untouched), no markdown edited (rule 1).
    no-hacky-shortcuts .... FAIL — Three instances of solving around the system rather than with it:
                      (1) `Modifier.offset(amount: Dp)` (NuclearSurface.kt:143-150) reimplements the
                      framework's `Modifier.offset { IntOffset }` using a raw `layout {}`, justified
                      by the comment "roundToPx needs a Density, and the placement block is not one."
                      That premise is false — the lambda overload of `Modifier.offset` has a
                      `Density` receiver, so `Modifier.offset { IntOffset(amount.roundToPx(), amount.roundToPx()) }`
                      is a direct, layout-neutral, deferred-to-placement replacement. A custom layout
                      node was added to work around a misreading of the API.
                      (2) The same four-modifier chunk chain is duplicated inline at Player.kt:1268,
                      1288, 1326 and 1753 instead of calling the abstraction the same commit
                      extracted for precisely this purpose.
                      (3) The `applicationId` rename was applied at its source
                      (app/build.gradle.kts:10) but its dependents were not swept —
                      `shortcuts.xml` in two source sets still names the old id (see
                      `no-regressions`), which is the symptom of a rename done by editing one string
                      rather than by grepping the tree for it.
                      Fix: replace the custom `Modifier.offset` with the stdlib lambda overload and
                      delete the incorrect comment; convert the four Player.kt chains to
                      `NuclearIconButton`; grep for `com.metrolist.music` in `res/` and
                      `AndroidManifest.xml` and fix every reference that means *applicationId*
                      rather than *namespace*.

Result: 11/20 PASS — 9 criteria need fixing

Failing criteria with fixes:
1. logic-correct: `NuclearFaceFill.Height` → `fillMaxHeight()` is inert under unbounded height, so the "Play all" button's face is ~16dp inside a 34dp shadow box (NuclearButton.kt:108-124 + NuclearSurface.kt:99, at NavigationTitle.kt:88). Size the face from the outer Box's measured size with a custom `Layout`, or at minimum add `Modifier.heightIn(min = size.height)` to the face.
2. edge-cases: `NuclearSurface`'s `enabled` has no visual treatment (NuclearSurface.kt:78, 110), so disabled prev/next are indistinguishable (Player.kt:1590, 1680); and pure black makes `shadow == background == Black` (Theme.kt:56 + NuclearPalette.kt:87). Move the disabled alpha into `NuclearSurface`; keep the original `backgroundInput` for `shadow` under pure black.
3. no-dead-code: 20 newly unused imports (Player.kt:31, NavigationTitle.kt:8/12/17/22/23/26, NewMenuComponents.kt:10/12/23/24, Queue.kt:47/84, ThemeScreen.kt:16/60/61/64/80, MainActivity.kt:61, ChipsRow.kt:34), an orphaned `SelectedThemeColorKey` with no writer but a live reader (AppearanceSettings.kt:155-161, 960), two dead params each in MiniPlayer.kt:1044-1046 and 1088-1090, and a duplicated `LaunchedEffect` at MainActivity.kt:608-612. Sweep imports, delete the orphaned preference path and dead params.
4. no-code-smell: `NuclearFaceFill` (NuclearSurface.kt:140) forces callers to know the surface's private layout, and `enabled` means different things in `NuclearSurface` vs `NuclearButton`/`NuclearIconButton`. Remove `NuclearFaceFill` from the public API by making the face fill unconditionally, and unify the `enabled` contract.
5. no-obvious-perf: `NuclearSurface` allocates an interaction source, a press collector and an `animateDpAsState` even when `onClick == null` (NuclearSurface.kt:72-81), inside `GridItem` (Items.kt:428) which backs 62 lazy-grid/row call sites. Skip the interaction plumbing when `onClick == null` or `!shadow`.
6. patterns-followed: `IntegrationCard.kt:67-71` got 1 of 4 radius branches and no border, unlike Material3SettingsGroup.kt:70-92 and Menu.kt:40-55; and Player.kt:1268, 1288, 1326, 1753 still hand-roll the chunk instead of using `NuclearIconButton`. Finish both conversions.
7. tests-exist: no tests for `contrastRatio`/`readableOn`/`mutedText`/`contentColorOn`/`NuclearThemeId.palette` (NuclearColorScheme.kt:114-174, NuclearPalette.kt:100), despite 137 existing JVM tests. Add `NuclearPaletteTest` asserting ≥4.5:1 for foreground and muted text across all 5 presets × light/dark.
8. no-regressions: `app/build.gradle.kts:10` renames the applicationId while `app/src/main/res/xml/shortcuts.xml:11,21` and `app/src/debug/res/xml/shortcuts.xml:13,21` still hardcode the old one — both launcher shortcuts break; and the conversions drop `minimumInteractiveComponentSize()` + `Role.Button` across ~12 controls. Fix the shortcut ids; add `.minimumInteractiveComponentSize()` and `role = Role.Button` to `NuclearSurface`.
9. no-hacky-shortcuts: `Modifier.offset(Dp)` (NuclearSurface.kt:143-150) reimplements `Modifier.offset { }` on a false premise about `Density`; the chunk chain is duplicated at Player.kt:1268/1288/1326/1753; the applicationId rename was not swept through `res/`. Use the stdlib overload, call the extracted component, and grep for the old id.

Unverified:
• I did not run the app or render a single frame. Every layout and colour claim (the `NuclearButton` shadow band, the invisible pure-black shadow, the disabled prev/next appearance) is a static reading of Compose's measurement rules, not an observation on a device or in a screenshot test.
• I did not run `./gradlew :app:assembleFossDebug`, the build command AGENTS.md actually specifies — only `compileFossDebugKotlin` and `testFossDebugUnitTest`. Resource merging, lint, and the launcher-icon/adaptive-icon drawable changes were therefore never exercised by a real packaging run.
• I did not verify that `BadgedBox` inside the now-38dp `NuclearIconButton` at MainActivity.kt:1084 still renders its update badge rather than having it clipped by the face's `.clip(shape)`.
• I did not verify the OKLCH→sRGB conversions in `NuclearPalette.kt` against nuclear's actual `global.css` / `packages/themes/src/basic` sources; I have no copy of that repo and took the hex values on trust.
• I did not check the three added `.ttf` files for licence compliance beyond confirming that `assets/licenses/OFL-*.txt` exist alongside them, nor confirm the variable-font `wght` axis actually resolves on API 26.
• I did not test the fork's behaviour for a user upgrading from an installed Metrolist build (the applicationId change makes it a separate app, so the orphaned `SelectedThemeColorKey` path may be unreachable in practice — I could not confirm either way).
